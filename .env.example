# Postgres connection string; leave empty to use the demo repository.
DATABASE_URL=
# ElevenLabs agent id; the server uses it to mint WebRTC conversation tokens.
ELEVENLABS_AGENT_ID=
# ElevenLabs API key; this stays on the server and never reaches the browser.
ELEVENLABS_API_KEY=
